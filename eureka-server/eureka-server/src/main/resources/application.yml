server:
  port: 8761

spring:
  application:
    name: eureka-server
  main:
    allow-bean-definition-overriding: true   # ✅ Fix for bean conflict

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    wait-time-in-ms-when-sync-empty: 0
